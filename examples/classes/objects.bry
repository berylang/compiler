class Shape {
    attributes[me]::
        int len;
        int brd;
    methods::
        func area() -> int {
            return len;
        }
}
run {
    Shape s = new Shape();
    s.len = 10;
    println(s.area());
}